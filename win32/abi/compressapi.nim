# generated by winmd2nim from Windows.Win32.winmd — compressapi (do not edit)
import ./[minwindef]
export minwindef

type

  # structs
  COMPRESS_ALLOCATION_ROUTINES* {.completeStruct.} = object
    Allocate*: PFN_COMPRESS_ALLOCATE
    Free*: PFN_COMPRESS_FREE
    UserContext*: pointer

  # typdefs
  COMPRESSOR_HANDLE* = distinct pointer
  DECOMPRESSOR_HANDLE* = COMPRESSOR_HANDLE
  PCOMPRESSOR_HANDLE* = ptr COMPRESSOR_HANDLE
  PCOMPRESS_ALLOCATION_ROUTINES* = ptr COMPRESS_ALLOCATION_ROUTINES
  PDECOMPRESSOR_HANDLE* = ptr COMPRESSOR_HANDLE

  # unscoped enums 
  COMPRESS_INFORMATION_CLASS* = int32

  # delegates
  PFN_COMPRESS_ALLOCATE* = proc (arg_1: pointer, arg_2: uint): pointer {.stdcall.}
  PFN_COMPRESS_FREE* = proc (arg_1: pointer, arg_2: pointer): void {.stdcall.}

const
  # free constants
  COMPRESS_ALGORITHM_INVALID*: int32 = 0'i32
  COMPRESS_ALGORITHM_LZMS*: int32 = 5'i32
  COMPRESS_ALGORITHM_MAX*: int32 = 6'i32
  COMPRESS_ALGORITHM_MSZIP*: int32 = 2'i32
  COMPRESS_ALGORITHM_NULL*: int32 = 1'i32
  COMPRESS_ALGORITHM_XPRESS*: int32 = 3'i32
  COMPRESS_ALGORITHM_XPRESS_HUFF*: int32 = 4'i32
  COMPRESS_RAW*: int32 = 536870912'i32

  # unscoped enum members
  COMPRESS_INFORMATION_CLASS_INVALID*: COMPRESS_INFORMATION_CLASS = 0'i32
  COMPRESS_INFORMATION_CLASS_BLOCK_SIZE*: COMPRESS_INFORMATION_CLASS = 1'i32
  COMPRESS_INFORMATION_CLASS_LEVEL*: COMPRESS_INFORMATION_CLASS = 2'i32

{.push dynlib: "cabinet".}
proc CloseCompressor*(CompressorHandle: COMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc CloseDecompressor*(DecompressorHandle: DECOMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc Compress*(CompressorHandle: COMPRESSOR_HANDLE, UncompressedData: pointer, UncompressedDataSize: uint, CompressedBuffer: pointer, CompressedBufferSize: uint, CompressedDataSize: ptr uint): BOOL {.sideEffect, importc, stdcall.}
proc CreateCompressor*(Algorithm: uint32, AllocationRoutines: ptr COMPRESS_ALLOCATION_ROUTINES, CompressorHandle: ptr COMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc CreateDecompressor*(Algorithm: uint32, AllocationRoutines: ptr COMPRESS_ALLOCATION_ROUTINES, DecompressorHandle: ptr COMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc Decompress*(DecompressorHandle: DECOMPRESSOR_HANDLE, CompressedData: pointer, CompressedDataSize: uint, UncompressedBuffer: pointer, UncompressedBufferSize: uint, UncompressedDataSize: ptr uint): BOOL {.sideEffect, importc, stdcall.}
proc QueryCompressorInformation*(CompressorHandle: COMPRESSOR_HANDLE, CompressInformationClass: COMPRESS_INFORMATION_CLASS, CompressInformation: pointer, CompressInformationSize: uint): BOOL {.sideEffect, importc, stdcall.}
proc QueryDecompressorInformation*(DecompressorHandle: DECOMPRESSOR_HANDLE, CompressInformationClass: COMPRESS_INFORMATION_CLASS, CompressInformation: pointer, CompressInformationSize: uint): BOOL {.sideEffect, importc, stdcall.}
proc ResetCompressor*(CompressorHandle: COMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc ResetDecompressor*(DecompressorHandle: DECOMPRESSOR_HANDLE): BOOL {.sideEffect, importc, stdcall.}
proc SetCompressorInformation*(CompressorHandle: COMPRESSOR_HANDLE, CompressInformationClass: COMPRESS_INFORMATION_CLASS, CompressInformation: pointer, CompressInformationSize: uint): BOOL {.sideEffect, importc, stdcall.}
proc SetDecompressorInformation*(DecompressorHandle: DECOMPRESSOR_HANDLE, CompressInformationClass: COMPRESS_INFORMATION_CLASS, CompressInformation: pointer, CompressInformationSize: uint): BOOL {.sideEffect, importc, stdcall.}
{.pop.}
