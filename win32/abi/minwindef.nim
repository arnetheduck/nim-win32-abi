# generated by winmd2nim from Windows.Win32.winmd — minwindef (do not edit)

type

  # structs
  FILETIME* {.completeStruct.} = object
    dwLowDateTime*: uint32
    dwHighDateTime*: uint32

  # typdefs
  ATOM* = distinct uint16
  BOOL* = distinct int32
  GLOBALHANDLE* = distinct pointer #[HANDLE]#
  HFILE* = distinct int32
  HGLOBAL* = distinct pointer #[HANDLE]#
  HINSTANCE* = distinct pointer
  HKEY* = distinct pointer
  HKL* = distinct pointer
  HLOCAL* = distinct pointer #[HANDLE]#
  HLSURF* = distinct pointer
  HMETAFILE* = distinct pointer
  HMODULE* = HINSTANCE
  HRGN* = distinct pointer
  HRSRC* = distinct pointer
  HSPRITE* = distinct pointer
  HSTR* = distinct pointer
  HTASK* = distinct pointer
  HWINSTA* = distinct pointer
  LOCALHANDLE* = distinct pointer #[HANDLE]#
  LPARAM* = distinct int
  LPBOOL* = ptr BOOL
  LPBYTE* = distinct ptr uint8
  LPDWORD* = distinct ptr uint32
  LPFILETIME* = ptr FILETIME
  LPHANDLE* = distinct pointer #[ptr HANDLE]#
  LPINT* = distinct ptr int32
  LPLONG* = distinct ptr int32
  LPWORD* = distinct ptr uint16
  LRESULT* = distinct int
  PBOOL* = ptr BOOL
  PBYTE* = distinct ptr uint8
  PDWORD* = distinct ptr uint32
  PFILETIME* = ptr FILETIME
  PFLOAT* = distinct ptr float32
  PHKEY* = ptr HKEY
  PINT* = distinct ptr int32
  PSZ* = distinct ptr int8
  PUCHAR* = distinct ptr uint8
  PUINT* = distinct ptr uint32
  PULONG* = distinct ptr uint32
  PUSHORT* = distinct ptr uint16
  PWORD* = distinct ptr uint16
  SPHANDLE* = distinct pointer #[ptr HANDLE]#
  WPARAM* = distinct uint

  # delegates
  FARPROC* = pointer
  NEARPROC* = pointer
  PROC* = pointer

const
  # free constants
  FALSE*: int32 = 0'i32
  MAX_PATH*: int32 = 260'i32
  STRICT*: int32 = 1'i32
  TRUE*: int32 = 1'i32

