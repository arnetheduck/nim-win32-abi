# generated by winmd2nim from Windows.Win32.winmd — stireg (do not edit)
import ./[devpropdef]
export devpropdef

const
  # free constants
  DEVPKEY_WIA_DeviceType*: DEVPROPKEY = DEVPROPKEY(fmtid: DEVPROPGUID(GUID()), pid: DEVPROPID(0x2'u32))
  DEVPKEY_WIA_USDClassId*: DEVPROPKEY = DEVPROPKEY(fmtid: DEVPROPGUID(GUID()), pid: DEVPROPID(0x3'u32))
  # IS_DIGITAL_CAMERA_STR = "IsDigitalCamera"  # TODO: wide string constant (utf-16)
  IS_DIGITAL_CAMERA_VAL*: int32 = 1'i32
  # REGSTR_VAL_BAUDRATE = "BaudRate"  # TODO: wide string constant (utf-16)
  REGSTR_VAL_BAUDRATE_A*: string = "BaudRate"
  # REGSTR_VAL_DATA_W = "DeviceData"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_DEVICESUBTYPE_W = "DeviceSubType"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_DEVICETYPE_W = "DeviceType"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_DEVICE_NAME_W = "DriverDesc"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_DEV_NAME_W = "DeviceName"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_DRIVER_DESC_W = "DriverDesc"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_FRIENDLY_NAME_W = "FriendlyName"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_GENERIC_CAPS_W = "Capabilities"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_GUID_W = "GUID"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_HARDWARE_W = "HardwareConfig"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_LAUNCHABLE_W = "Launchable"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_LAUNCH_APPS_W = "LaunchApplications"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_SHUTDOWNDELAY_W = "ShutdownIfUnusedDelay"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_TYPE_W = "Type"  # TODO: wide string constant (utf-16)
  # REGSTR_VAL_VENDOR_NAME_W = "Vendor"  # TODO: wide string constant (utf-16)
  # STI_DEVICE_VALUE_DEFAULT_LAUNCHAPP = "DefaultLaunchApp"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_DEFAULT_LAUNCHAPP_A*: string = "DefaultLaunchApp"
  # STI_DEVICE_VALUE_DISABLE_NOTIFICATIONS = "DisableNotifications"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_DISABLE_NOTIFICATIONS_A*: string = "DisableNotifications"
  # STI_DEVICE_VALUE_ICM_PROFILE = "ICMProfile"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_ICM_PROFILE_A*: string = "ICMProfile"
  # STI_DEVICE_VALUE_ISIS_NAME = "ISISDriverName"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_ISIS_NAME_A*: string = "ISISDriverName"
  # STI_DEVICE_VALUE_TIMEOUT = "PollTimeout"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_TIMEOUT_A*: string = "PollTimeout"
  # STI_DEVICE_VALUE_TWAIN_NAME = "TwainDS"  # TODO: wide string constant (utf-16)
  STI_DEVICE_VALUE_TWAIN_NAME_A*: string = "TwainDS"
  # SUPPORTS_MSCPLUS_STR = "SupportsMSCPlus"  # TODO: wide string constant (utf-16)
  SUPPORTS_MSCPLUS_VAL*: int32 = 1'i32

