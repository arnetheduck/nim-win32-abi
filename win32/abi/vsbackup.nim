# generated by winmd2nim from Windows.Win32.winmd — vsbackup (do not edit)
import ./[vswriter, unknwnbase, winnt, minwindef, wtypes, guiddef, vss]
export vswriter, unknwnbase, winnt, minwindef, wtypes, guiddef, vss

type

  # structs
  VSS_COMPONENTINFO* {.completeStruct.} = object
    `type`*: VSS_COMPONENT_TYPE
    bstrLogicalPath*: BSTR
    bstrComponentName*: BSTR
    bstrCaption*: BSTR
    pbIcon*: ptr uint8
    cbIcon*: uint32
    bRestoreMetadata*: bool
    bNotifyOnBackupComplete*: bool
    bSelectable*: bool
    bSelectableForRestore*: bool
    dwComponentFlags*: uint32
    cFileCount*: uint32
    cDatabases*: uint32
    cLogFiles*: uint32
    cDependencies*: uint32

  # typdefs
  PVSSCOMPONENTINFO* = ptr VSS_COMPONENTINFO

  # interfaces
  IVssBackupComponents* = distinct object
  IVssBackupComponentsEx* = distinct object
  IVssBackupComponentsEx2* = distinct object
  IVssBackupComponentsEx3* = distinct object
  IVssBackupComponentsEx4* = distinct object
  IVssExamineWriterMetadata* = distinct object
  IVssExamineWriterMetadataEx* = distinct object
  IVssExamineWriterMetadataEx2* = distinct object
  IVssWMComponent* = distinct object
  IVssWriterComponentsExt* = distinct object

const
  # free constants
  VSS_SW_BOOTABLE_STATE*: int32 = 1'i32

{.push dynlib: "vssapi".}
proc CreateVssBackupComponentsInternal*(ppBackup: ptr IVssBackupComponents): HRESULT {.sideEffect, importc, stdcall.}
proc CreateVssExamineWriterMetadataInternal*(bstrXML: BSTR, ppMetadata: ptr IVssExamineWriterMetadata): HRESULT {.sideEffect, importc, stdcall.}
proc GetProviderMgmtInterfaceInternal*(ProviderId: VSS_ID, InterfaceId: GUID, ppItf: ptr IUnknown): HRESULT {.sideEffect, importc, stdcall.}
proc IsVolumeSnapshottedInternal*(pwszVolumeName: PCWSTR, pbSnapshotsPresent: ptr BOOL, plSnapshotCapability: ptr int32): HRESULT {.sideEffect, importc, stdcall.}
proc ShouldBlockRevertInternal*(wszVolumeName: PCWSTR, pbBlock: ptr bool): HRESULT {.sideEffect, importc, stdcall.}
proc VssFreeSnapshotPropertiesInternal*(pProp: ptr VSS_SNAPSHOT_PROP) {.sideEffect, importc, stdcall.}
{.pop.}
