# generated by winmd2nim from Windows.Win32.winmd — windowsuixamlhostingreferencetracker (do not edit)

type

  # typdefs
  TrackerHandle* = distinct pointer

  # unscoped enums 
  XAML_REFERENCETRACKER_DISCONNECT* = int32

  # interfaces
  IFindReferenceTargetsCallback* = distinct object
  IReferenceTracker* = distinct object
  IReferenceTrackerExtension* = distinct object
  IReferenceTrackerHost* = distinct object
  IReferenceTrackerManager* = distinct object
  IReferenceTrackerTarget* = distinct object
  ITrackerOwner* = distinct object

const
  # unscoped enum members
  XAML_REFERENCETRACKER_DISCONNECT_DEFAULT*: XAML_REFERENCETRACKER_DISCONNECT = 0'i32
  XAML_REFERENCETRACKER_DISCONNECT_SUSPEND*: XAML_REFERENCETRACKER_DISCONNECT = 1'i32

